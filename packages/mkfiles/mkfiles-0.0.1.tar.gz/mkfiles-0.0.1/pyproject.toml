[project]
name = "mkfiles"
version = "0.0.1"
authors = [{ name = "Rodrigo Mayorga V.", email = "rmayorgav@rmayorgav.com" }]
description = ""
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
]

[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project.scripts]
mkfiles = "mkfiles.__main__:main"