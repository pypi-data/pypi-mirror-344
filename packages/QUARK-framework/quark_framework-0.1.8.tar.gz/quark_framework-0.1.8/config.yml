plugins: ["quark_plugin_tsp", "quark_plugin_devices_dwave"]

pipeline1:
  &pipeline1 [
    ["tsp_graph_provider", "tsp_graph_provider": { nodes: 5, seed: 43 }],
    "tsp_qubo_mapping_dnx",
    [
      "simulated_annealer": { num_reads: 1 },
      "simulated_annealer": { num_reads: 1000 },
    ],
  ]

pipeline2: &pipeline2 ["tsp_graph_provider", "classical_tsp_solver"]
# pipelines: [ *pipeline1, *pipeline2 ]
pipelines: [*pipeline1, *pipeline2]
