[project]
name = "jira-mcp"
version = "0.1.3"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "mcp[cli]>=1.6.0",
]
[project.scripts]
jira-mcp = "main:main"
[tool.setuptools]
py-modules = ["main", "server"]
packages = ["tools", "utils"]