from setuptools import setup, find_packages

setup(
    name='deep_node',
    version='0.5',
    author='John Doe',
    description='Library of converted notebooks',
    packages=find_packages(),
    install_requires=[],
)
