[project]
name = "mcp-jira"
version = "0.0.2"
description = "MCP server for Jira integration"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.6.0",
    "httpx>=0.24.0",
]

[project.scripts]
mcp-jira = "mcp_jira.__init__:main"
