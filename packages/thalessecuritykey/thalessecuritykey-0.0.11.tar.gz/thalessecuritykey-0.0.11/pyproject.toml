[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "thalessecuritykey"
description = "Library accessing Thales Security keys (FIDO & PKI)"
readme = "README.md"
version = "0.0.11"
dependencies = [
    "pyscard==2.2.1",
    "fido2",
    "fido2[PCSC]"
]