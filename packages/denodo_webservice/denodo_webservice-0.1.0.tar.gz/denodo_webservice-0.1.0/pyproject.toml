[project]
name = "denodo_webservice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "David Gallay", email = "david.gallay.96@gmail.com" },
    { name = "David Gallay", email = "david.gallay@nagra.com" },
]
packages = [
    {include = "denodo_webservice", from="src"}]
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
