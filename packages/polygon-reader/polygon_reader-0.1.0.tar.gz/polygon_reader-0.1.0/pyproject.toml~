[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "polydata"  # Package name, used in imports and PyPI
version = "0.1.0"
description = "A simple package to read financial data from Polygon.io"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.25.1",  # For HTTP requests to Polygon.io
    "pandas>=1.5.0",     # For data handling, if used
]
authors = [{name = "Igor Rivin", email = "igor@dimensionreducers.com"}]
license = {text = "MIT"}

[project.urls]
Homepage = "https://github.com/igorrivnn/polygon"
