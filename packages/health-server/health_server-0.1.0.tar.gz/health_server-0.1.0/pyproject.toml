[project]
name = "health-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "liming", email = "liming@addcn.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[project.scripts]
health-server = "health_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
