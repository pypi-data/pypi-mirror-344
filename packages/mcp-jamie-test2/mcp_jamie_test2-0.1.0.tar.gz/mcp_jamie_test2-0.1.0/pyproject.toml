[project]
name = "mcp-jamie-test2"
version = "0.1.0"
description = "MCP server 测试使用"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.6.0",
]

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
weather = "mcp_jamie_test2:main"