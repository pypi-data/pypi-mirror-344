[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "friendsbalt"
version = "0.1.42"
authors = [
  { name="Joel Hammer", email="jhammer@friendsbalt.org" },
]
description = "Helper code for Friends School of Baltimore CS curriculum"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "matplotlib",
]

[project.urls]
"Homepage" = "https://github.com/yourusername/friendsbaltcs"
"Bug Tracker" = "https://github.com/yourusername/friendsbaltcs/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/friendsbalt"]