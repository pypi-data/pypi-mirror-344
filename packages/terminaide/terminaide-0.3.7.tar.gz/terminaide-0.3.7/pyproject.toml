[tool.poetry]
name = "terminaide"
version = "0.3.7"
description = "Serve Python CLI applications in the browser using ttyd"
authors = ["Alex Basile <basileaw@gmail.com>"]
license = "MIT"
readme = "README.md"
repository = "https://github.com/bazeindustries/terminaide"
packages = [{ include = "terminaide" }]

[tool.poetry.dependencies]
python = ">=3.12"
fastapi = ">=0.104.0"
jinja2 = ">=3.1.2"
httpx = ">=0.25.0,<0.28.0"
httpcore = ">=0.17.3,<0.19.0"
websockets = ">=12.0"
pydantic = ">=2.4.2"
readchar = ">=4.2.1"
uvicorn = ">=0.24.0"
rich = ">=14.0.0"
bs4 = ">=0.0.2"

[tool.poetry.group.dev.dependencies]
docker = ">=7.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
