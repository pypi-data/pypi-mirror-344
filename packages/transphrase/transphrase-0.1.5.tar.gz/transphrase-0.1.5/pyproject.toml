[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[project]
name = "transphrase"
version = "0.1.5"
description = "AI-powered web novel translation tool"
authors = [
    {name = "shinyPy", email = "adveniscool@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "rich>=14.0.0",
    "tenacity>=9.1.0",
    "openai>=1.70.0",
    "readchar>=4.2.1",
    "sqlalchemy>=2.0.40",
    "pyyaml>=6.0.2",
    "langdetect>=1.0.9",
    "ebooklib>=0.18",
    "lxml>=5.4.0",
    "beautifulsoup4>=4.12.2",
]

[project.urls]
Homepage = "https://github.com/shinyPy/TransPhrase"
Issues = "https://github.com/shinyPy/TransPhrase/issues"
Documentation = "https://github.com/shinyPy/TransPhrase#readme"
Source = "https://github.com/shinyPy/TransPhrase"

[project.scripts]
transphrase = "transphrase.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["transphrase"]

[tool.black]
line-length = 100
target-version = ["py39"]

[tool.isort]
profile = "black"
line_length = 100

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
