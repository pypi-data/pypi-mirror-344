[tool.poetry]
name = "acace_llm_adapter"
version = "0.1.0"
description = "LLM adapter utilities for ACACE"
authors = ["ACACE Team <team@acace.ai>"]
readme = "README.md"
packages = [{include = "acace_llm_adapter", from = "src"}]

[tool.poetry.dependencies]
python = "^3.8"
openai = "^1.12.0"
anthropic = "^0.18.1"
numpy = "^1.26.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api" 