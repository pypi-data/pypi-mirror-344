[build-system]
requires = ["setuptools>=40.8.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fastdomaincheck-mcp-server"
version = "0.1.0"
description = "MCP Server for checking domain name availability using WHOIS and DNS."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
    { name = "Bingal", email = "bingalsky@gmail.com" },
]
keywords = ["mcp", "domain", "whois", "dns", "availability", "check"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Topic :: Internet :: Name Service (DNS)",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Operating System :: OS Independent",
]
dependencies = [
    "mcp[cli]>=1.6.0",
    "python-whois>=0.9.5",
    "uvicorn[standard]>=0.20.0",
]

[project.urls]
Homepage = "https://www.bingal.com/"
Repository = "https://github.com/bingal/fastdomaincheck-mcp-server-python"

[project.scripts]
fastdomaincheck-server = "fastdomaincheck_mcp_server.server:run_server"

[tool.setuptools.packages.find]
where = ["src"]

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple"
default = true
