def download_file(repo_name, path, file_name):
    print(f"Downloaded the file {file_name} from the repo {repo_name} and from the path {path}")


def upload_file(repo_name, path, file_name):
    print(f"Uploaded the file {file_name} to the repo {repo_name} and onto the path {path}")
