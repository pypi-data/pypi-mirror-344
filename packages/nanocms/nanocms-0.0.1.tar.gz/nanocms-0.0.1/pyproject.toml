[project]
name = "nanocms"
version = "0.0.1"
description = "🌈 A lightweight color management system written in Python - placeholder"
license = "MIT"
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"