[project]
name = "mcp-yx-weather"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "御雪", email = "wb-spf530106@alibaba-inc.com" }
]
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
]

[project.scripts]
mcp-yx-weather = "mcp_yx_weather:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
