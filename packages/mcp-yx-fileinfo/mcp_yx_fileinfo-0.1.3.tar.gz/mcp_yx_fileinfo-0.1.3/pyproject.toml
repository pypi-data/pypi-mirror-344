[project]
name = "mcp-yx-fileinfo"
version = "0.1.3"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "御雪", email = "wb-spf530106@alibaba-inc.com" }
]
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.6.0",
]

[project.scripts]
mcp-yx-fileinfo = "mcp_yx_fileinfo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
