[project]
name = "full-mcp-server"
version = "0.1.5"
description = "Creating MCP project with theS SDK wizard"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
 "boto3>=1.38.5",
 "mcp>=1.6.0",
 "python-dotenv>=1.1.0",
]
[[project.authors]]
name = "Sakthivel Chellapparimanam"
email = "srcsakthivel@gmail.com"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
full-mcp-server = "full_mcp_server.server:main"
