[build-system]
requires = ["hatchling >= 1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["FlexATA"]

[project]
name = "FlexATA"
version = "0.0.12"
authors = [
  { name="Yan Fu", email="yanfu.phd@gmail.com" },
]
description = "A flexible mixed integer program for automated test assembly (ATA) in Python."
dependencies =[
  "numpy >= 2.2",
  "pandas >= 2.2",
  "openpyxl >= 3.1",
  "PuLP >= 3.1",
  "cplex >= 22.1"
]
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

license = "MIT"
license-files = ["LICEN[CS]E*"]

[project.urls]
Homepage = "https://github.com/frankyanfu/FlexATA"
Issues = "https://github.com/frankyanfu/FlexATA/issues"