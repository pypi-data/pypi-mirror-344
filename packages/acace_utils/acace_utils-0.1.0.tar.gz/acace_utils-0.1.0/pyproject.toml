[tool.poetry]
name = "acace_utils"
version = "0.1.0"
description = "Utility functions for ACACE"
authors = ["ACACE Team <team@acace.ai>"]
readme = "README.md"
packages = [{include = "acace_utils", from = "src"}]

[tool.poetry.dependencies]
python = "^3.8"
numpy = "^1.26.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api" 