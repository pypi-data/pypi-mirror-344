[project]
name = "Pylightpanda"
version = "0.1.0"
description = "A Python package wrapping the lightpanda binary to fetch HTML content."
readme = "README.md"
requires-python = ">=3.6"
license = { file = "LICENSE" }
authors = [
    { name = "Roycicd", email = "abc@abc.com" }
]
dependencies = []

[project.urls]
Homepage = "https://github.com/yourusername/pylightpanda"
Repository = "https://github.com/yourusername/pylightpanda.git"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["pylightpanda"]
license-files = []

[tool.setuptools.package-data]
"pylightpanda" = ["bin/lightpanda"]