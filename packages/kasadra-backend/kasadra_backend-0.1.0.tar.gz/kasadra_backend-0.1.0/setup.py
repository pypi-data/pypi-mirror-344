from setuptools import setup, find_packages

setup(
    name='kasadra-backend',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # Add your runtime dependencies here
    ],
)
