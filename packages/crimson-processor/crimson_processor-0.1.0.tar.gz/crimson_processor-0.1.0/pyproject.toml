[project]
name = "crimson-processor"
version = "0.1.0"
description = ""
authors = [
    {name = "crimson206",email = "sisung.kim1@gmail.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[tool.poetry]
packages = [
  { include = "crimson", from = "src" }
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
