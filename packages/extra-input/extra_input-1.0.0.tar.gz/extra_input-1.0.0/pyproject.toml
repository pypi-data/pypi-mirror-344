[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "extra-input"
version = "1.0.0"
description = "Various functions to get input from the user"
authors = [{ name = "trinary", email = "lpowyt3@gmail.com" }]
classifiers = [
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
]
dependencies = ["roman"]
requires-python = ">=3.9"


[tool.setuptools.packages.find]
where = ["."]