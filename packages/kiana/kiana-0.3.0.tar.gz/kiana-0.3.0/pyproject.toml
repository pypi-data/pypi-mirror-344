[project]
name = "kiana"
version = "0.3.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Xiaohua Zhang", email = "xiaohua.dev@gmail.com" }
]
requires-python = ">=3.12"
# dependencies = [
#     "bar",
#     "foo",
# ]

[project.scripts]
kiana = "kiana:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.workspace]
members = ["packages/foo", "packages/bar"]

# [tool.uv.sources]
# foo = { workspace = true }
# bar = { workspace = true }
