[tool.poetry]
name = "xmlvault"
version = "0.0.32"
description = "xml utility"
authors = ["Ankit K <ankit48365@gmail.com>"]
license = "MIT"
readme = "README.md"

packages = [ { include = "app", from = "." } ]

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.2.3"
requests = "^2.32.3"
pyyaml = "^6.0.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts] 
xmlvault = "app.cli:main"