[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "utkarsh_odd_vowels"  # Must be unique on PyPI!
version = "0.1.0"
description = "A simple package to check odd numbers and count vowels/consonants"
authors = [
  { name="Your Name", email="your.email@example.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"

[project.urls]
Homepage = "https://pypi.org/project/mypackage/"
