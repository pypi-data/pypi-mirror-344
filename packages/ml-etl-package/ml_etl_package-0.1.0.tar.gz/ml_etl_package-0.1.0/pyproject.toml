# pyproject.toml
[project]
name = "ml-etl-package"
version = "0.1.0"
description = "A simple ML pipeline package with ETL steps using scikit-learn"
readme = "README.md"
authors = [
    { name = "Your Name", email = "you@example.com" }
]
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "scikit-learn",
    "joblib"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"