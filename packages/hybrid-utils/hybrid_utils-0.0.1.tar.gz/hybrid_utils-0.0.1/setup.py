from setuptools import setup, find_packages

setup(
    name='hybrid-utils',
    version='0.0.1',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.7',
    include_package_data=True,
)