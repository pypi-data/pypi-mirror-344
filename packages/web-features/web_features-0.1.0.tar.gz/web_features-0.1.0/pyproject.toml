[project]
name = "web-features"
version = "0.1.0"
description = "Library for working with web-features data"
authors = [
  {name = "James Graham", email = "james@hoppipolla.co.uk"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx[http2]>=0.28.1",
    "pydantic>=2.11.3",
]

[project.optional-dependencies]
test = [
  "mypy==1.10.0",
  "ruff==0.4.8",
  "pytest==8.3.4",
  "pytest-ruff==0.3.2",
]
