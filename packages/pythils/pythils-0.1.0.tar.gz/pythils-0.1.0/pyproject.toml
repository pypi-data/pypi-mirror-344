[project]
name = "pythils"
version = "0.1.0"
description = "A Python utilities package."
readme = "README.md"

requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"







