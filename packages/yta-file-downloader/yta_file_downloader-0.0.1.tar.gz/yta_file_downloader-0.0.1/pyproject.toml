[project]
name = "yta-file-downloader"
version = "0.0.1"
description = ""
authors = [
    {name = "danialcala94",email = "danielalcalavalera@gmail.com"}
]
readme = "README.md"
requires-python = "==3.9"
dependencies = [
    "yta_general_utils (>=0.0.250,<1.0.0)",
    "yta_google_drive_downloader (>=0.0.1,<1.0.0)",
    # TODO: Please, avoid this by implementing helper methods
    # in the general library to avoid implementing the By here
    "selenium (>=4.18.1,<5.0.0)",
    "requests (>=2.32.3,<3.0.0)"
]

[tool.poetry]
packages = [{include = "yta_file_downloader", from = "src"}]

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
