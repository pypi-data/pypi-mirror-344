from setuptools import setup, find_packages

setup(
    name='mcp-custom',  
    version='0.1.0-custom',  # Add suffix if needed
    description='Custom version of MCP Python SDK',
    author='tester',
    packages=find_packages(),
    install_requires=[
        # keep these as in original or modify
    ],
)
