[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bertho-toolkit"
version = "1.0.0"
description = "Collection of utilities for logging, file management, and environment variable handling."
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Matheus Bertho", email = "matheus.berthot@gmail.com" }]
requires-python = ">=3.8"
dependencies = []

keywords = ["toolkit", "utilities", "environment", "files", "logging", "terminal"]
