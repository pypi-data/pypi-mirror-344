[project]
name = "mcp-package"
version = "0.1.1"
description = "MCP Server built with FastAPI MCP"
authors = [
  { name="madhurya", email="madhurya3199@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.100",
    "fastapi-mcp>=0.1.0",
    "uvicorn[standard]>=0.22.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build" 