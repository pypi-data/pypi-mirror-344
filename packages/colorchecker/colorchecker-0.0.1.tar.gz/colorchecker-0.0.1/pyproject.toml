[project]
name = "colorchecker"
version = "0.0.1"
description = "🎨 Detect and calibrate ColorChecker charts in images automatically - placeholder"
license = "MIT"
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"