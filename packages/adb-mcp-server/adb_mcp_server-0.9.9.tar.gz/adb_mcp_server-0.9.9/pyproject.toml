[project]
name = "adb-mcp-server"
version = "0.9.9"
description = "adb command tools with mcp"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
   "mcp>=1.6.0",
]
[project.scripts]
adb-mcp-server = "main:main"  
