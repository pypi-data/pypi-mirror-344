
[project]
name = "requestgo"
version = "0.1.0"
description = "A no-dependency HTTP request library"
authors = [{name = "Cansila"}]
license = "MIT"
requires-python = ">=3.6"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
